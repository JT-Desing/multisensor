# Monitoring Technology

Static scroll animation for DALOG multisensor monitoring categories.

Published with GitHub Pages from the `main` branch.

Elementor-ready embed:

- `elementor/monitoring-technology-elementor.html`
- Copy the full file into an Elementor HTML widget. Its CSS/JS is scoped with the `dalog-mt` prefix.
